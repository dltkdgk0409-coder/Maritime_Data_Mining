# Type II 문제 풀이에서 탐색적 데이터 분석(EDA)이 필요한 이유

Type II 문제는 단순히 코드를 실행하는 것이 아니라, 데이터를 이해한 뒤 전처리와 모델링을 설계하는 과정이다. 이 과정에서 EDA는 다음과 같은 이유로 반드시 필요하다.

## 1) 데이터 상태를 먼저 파악해야 올바른 전처리 방향을 결정할 수 있다
결측치가 얼마나 있는지, 범주형 변수가 있는지를 확인해야 대체 방법과 인코딩 방식을 선택할 수 있기 때문이다.

## 2) 전처리 코드에 대한 논리적 근거가 생긴다
EDA 없이 전처리를 진행하면 왜 이렇게 했는지를 설명할 수 없다. CBT 시험에서는 풀이 과정의 합리성도 중요하기 때문에 근거 있는 전처리가 필요하다.

## 3) 잘못된 모델 선택을 사전에 방지할 수 있다
변수 간 선형성이 약한 데이터에 선형 모델만 적용하거나, 변수 스케일 차이가 큰데 정규화를 생략하는 실수를 줄일 수 있다.

## 4) 검증 데이터 분할 시 데이터 누수(data leakage)를 방지할 수 있다
데이터 구조를 먼저 이해해야 train/test 분리 과정에서 오류를 막을 수 있다.

## 5) 타깃 분포를 확인하여 평가지표 선택의 근거를 마련할 수 있다
타깃 불균형이 심한 경우 Accuracy보다 ROC-AUC나 F1-Score를 우선적으로 고려해야 한다.

## 결론
EDA는 단순한 절차가 아니라 이후 모든 단계를 합리적으로 설계하기 위한 핵심 의사결정 단계이다.
